package com.taskmanager.client;

import net.fabricmc.api.ClientModInitializer;

public class TaskManagerModClient implements ClientModInitializer {
	@Override
	public void onInitializeClient() {
		// 客户端专属逻辑（如独立渲染 UI）将在此初始化。
	}
}
